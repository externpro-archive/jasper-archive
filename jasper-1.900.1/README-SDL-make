to make a debug version:
./configure; cd src/libjapser; gmake
cp .libs/libjasper.a ../../../lib/_SunOS_Debug/libjasperd.lib # contrib lib

to make a release version:
rm -rf jasper; svn update (wipe out any trace of the debug build); cd jasper
./configure CFLAGS=-O; cd src/libjasper; gmake
cp .libs/libjasper.a ../../../lib/_SunOS_Release/

Copy the release (libjasper.a) and debug (libjasperd.lib) from contrib lib
directory to sdlExtern/lib

